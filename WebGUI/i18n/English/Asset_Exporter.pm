package WebGUI::i18n::English::Asset_Exporter;

use strict;

our $I18N = {
    'assetName' => {
        message     => q|Exporter|,
        lastUpdated => 0,
        context     => q|asset name|,
    },

    'add data source' => {
        message     => q|Add a data source|,
    },

    'configured data sources' => {
        message     => q|Configured data sources|,
    },

    'export options' => {
        message     => q|Export options:|,
    },

    'export as html' => {
        message     => q|Export as HTML|,
    },

    'export as xls' => {
        message     => q|Export as XLS|,
    },

    'export as csv' => {
        message     => q|Export as CSV|,
    },

    'templateId label' => {
        message     => 'Exporter template',
    },

    'templateId label description' => {
        message     => q|The template that lays out the expoter main screen|,
    },
};

1;

