package WebGUI::i18n::Dutch::Asset_Exporter;

use strict;

our $I18N = {
    'assetName' => {
        message     => q|Exporter|,
        lastUpdated => 0,
        context     => q|asset name|,
    },

    'add data source' => {
        message     => q|Data source toevoegen|,
    },

    'configured data sources' => {
        message     => q|Geconfigureerde data sources|,
    },

    'export options' => {
        message     => q|Export opties:|,
    },

    'export as html' => {
        message     => q|Export als HTML|,
    },

    'export as xls' => {
        message     => q|Export als XLS|,
    },

    'export as csv' => {
        message     => q|Export als CSV|,
    },

    'templateId label' => {
        message     => 'Exporter template',
    },

    'templateId label description' => {
        message     => q|De template voor het Exporter hoofdscherm.|,
    },
};

1;

